import MovieList from "../../features/ui/MovieList";
import React from "react";

const Home: React.FC = () => {
    return (
        <div>
            <MovieList />
        </div>
    );
};

export default Home;
